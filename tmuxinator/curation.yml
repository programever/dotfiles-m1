name: curation
root: ~/Workspace/backoffice/modules/curation

windows:
  - editor: nvim
  - bash: git fetch --all && git status
  - lint: print -S "npm run lint"
  - server: print -S "npm run start"

